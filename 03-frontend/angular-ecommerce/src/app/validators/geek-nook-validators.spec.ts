import { GeekNookValidators } from './geek-nook-validators';

describe('GeekNookValidators', () => {
  it('should create an instance', () => {
    expect(new GeekNookValidators()).toBeTruthy();
  });
});
